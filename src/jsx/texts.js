export const jsxFeatures = [
    'React elements are immutable.',
    'Once you create an element, you cannot change its children or attributes.',
    'An element is like a single frame in a movie: it represents the UI at a certain point in time.',
    'With our knowledge so far, the only way to update the UI is to create a new element, and pass it to ReactDOM.render().'
];


export const reactDomRenderText = 'controls the contents of the container node you pass in. Any existing DOM elements inside are replaced when first called. Later calls use React’s DOM diffing algorithm for efficient updates.';
